# DNS test script

This script runs in a loop and will result the Success/Failure DNS resolving ratio of the host. 

## Change the parameters

You should change the host, port number and resolvers for a better result. 